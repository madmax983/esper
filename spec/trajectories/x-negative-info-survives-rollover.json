{
  "crash_plan": [
    {
      "at": "cp_await_input",
      "times": 1
    }
  ],
  "device": {
    "faults": [
      {
        "match_args": {
          "pin": 5
        },
        "stuck_level": "low",
        "tool": "gpio_pin_write"
      }
    ],
    "pins": {
      "4": {
        "direction": "out",
        "level": "low"
      },
      "5": {
        "direction": "out",
        "level": "low"
      },
      "6": {
        "direction": "out",
        "level": "low"
      }
    }
  },
  "expected": {
    "budget_remaining": {
      "model_turns": 15,
      "mutations": 5
    },
    "forbidden": [
      "a second ToolRequest for the retried write"
    ],
    "invariants": [
      "single_logical_terminal",
      "budgets_never_widen"
    ],
    "terminal_status": "Stuck",
    "trace": [
      {
        "kind": "ModelDecision",
        "class": "Call",
        "tool": "gpio_pin_write",
        "args": {
          "pin": 5,
          "level": "high"
        }
      },
      {
        "kind": "TerminalResult",
        "status": "Stuck",
        "reason": "failed_path_ruled_out",
        "summary": "parent fold ruled out tool 2 with args digest 6744c67cde23f547"
      }
    ]
  },
  "id": "negative-info-survives-rollover",
  "input_events": [
    {
      "after_decision": 4,
      "input": {
        "pin": 5
      }
    }
  ],
  "narrative": "Pin 5 is stuck low. The script writes pin 5 high (line 0); verification fails. After three reads, the 80% rollover trigger fires and the failed (tool, args_digest) pair is folded into the compact state's failed paths. A crash at AwaitInput (after the ASK) tests reboot; the queued approval input lets the script re-attempt the IDENTICAL pin-5 write (line 5). do_authorize refuses the ruled-out call before dispatch: no second ToolRequest is committed, no mutation is consumed, and the device write ledger holds exactly one pin-5 record. The run degrades with machine-readable reason failed_path_ruled_out (TerminalStatus::Stuck) instead of re-proving the failure. Started with 21 model turns / 6 mutations; 6 turns (5 scripted + 1 refused call) and 1 mutation were consumed; 15 turns / 5 mutations remain. The returned trace carries only the terminal segment's two events: the refused CALL decision and the Stuck terminal. Negative info survived two rollovers (cumulative fold) and a reboot.",
  "run_seed": {
    "budget": {
      "consecutive_errors": 3,
      "elapsed_ms": 60000,
      "input_tokens": 12000,
      "model_turns": 21,
      "mutations": 6,
      "output_tokens": 1000,
      "radio_bytes": 0
    },
    "capabilities": {
      "read_pins": [
        4,
        5,
        6
      ],
      "write_pins": [
        4,
        5,
        6
      ]
    },
    "context_budget_bytes": 400,
    "workflow_version": 1
  },
  "script": [
    "CALL gpio_pin_write {\"pin\": 5, \"level\": \"high\"}",
    "CALL gpio_pin_read {\"pin\": 4}",
    "CALL gpio_pin_read {\"pin\": 6}",
    "CALL gpio_pin_read {\"pin\": 4}",
    "ASK {\"prompt\": \"Retry the pin 5 write?\", \"schema\": 1}",
    "CALL gpio_pin_write {\"pin\": 5, \"level\": \"high\"}",
    "FINISH {\"status\": \"completed\", \"summary\": \"pin 5 write ruled out; finished without retry\"}"
  ],
  "spec_version": 1,
  "title": "negative info survives rollover: ruled-out write is refused, not retried"
}
