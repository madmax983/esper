[package]
name = "esper-eval"
version = "0.1.0"
edition = "2021"
description = "Esper E0/E1 eval: host-only golden-fixture runner, fault injector, scoring."
license = "MIT"

[dependencies]
# Workspace siblings: the runtime under test and its core types.
esper-core = { path = "../esper-core" }
esper-runtime = { path = "../esper-runtime" }
thiserror = "2"

[lints]
workspace = true
